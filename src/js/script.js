;(function($){
    $(document).ready(function() {

        // =============================================
        // Global variables
        // =============================================

        var $body = $('body');


        // =============================================
        // Initialise toggles globally
        // =============================================

        //var toggle = window.toggle = new ChopChop.Toggle('body');
        ChopChop.init();

    });
}(jQuery));
