;(function($){
    $(document).ready(function() {

        // =============================================
        // Global variables
        // =============================================

        var $body = $('body');


        // =============================================
        // Initialise toggles globally
        // =============================================

        var toggle = new ChopChop.Toggle('body');

    });
}(jQuery));
